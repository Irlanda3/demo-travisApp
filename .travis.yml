language: python
python:
  -3.7.1
os: windows
cache: yarn

install:
  - yarn intall

script: 
  -yarn test
  -yarn run build

deploy:
  provider: pages
  cleanup: true
  token: $GITHUB_TOKEN
  on:
    branch: master


     


